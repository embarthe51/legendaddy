<%#= render 'activities/back_arrow' %>
<%= sanitize activity.format_reviews %>
<% if action_name == "show" && activity.reviews.any? %>
  <%= link_to "#{activity.reviews.count} avis", activity_reviews_path(activity), data: { "popup-target": "trigger", action: "click->popup#toggle touch->popup#toggle"}%>
<% end %>
